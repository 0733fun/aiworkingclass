# 新手操作手册 —— 从零把 0733.fun 上线

这份手册假设你**从来没接触过建站**，不需要懂代码，不需要安装任何软件，**全程不用命令行**。

照着做，一步一步来，遇到看不懂的词就翻到最后的「术语小词典」。

---

# 一、先认识四个词（用人话解释）

在开始之前，你只需要理解这四个东西是什么：

| 词 | 用人话解释 | 打个比方 |
| --- | --- | --- |
| **网站文件** | 一堆文本文件，网站的全部内容都在里面 | 一本书的稿子 |
| **GitHub** | 一个免费存放文件的网站 | 一个网盘，但所有人都能看到里面的内容 |
| **Cloudflare Pages** | 一个免费服务，它会自动读取你 GitHub 上的文件，把它们变成一个能打开的网站 | 印刷厂 + 书店，自动把你的稿子变成书并摆上架 |
| **域名** | 网站的地址，也就是 `0733.fun` | 书店的门牌号 |

**它们怎么串起来：**

```
你的网站文件  →  存到 GitHub  →  Cloudflare Pages 自动读取并生成网站  →  绑定 0733.fun
   （稿子）        （网盘）              （印刷厂）                    （门牌号）
```

**好处是**：以后你想发新文章，只需要在 GitHub 网页上改一个文件、点一下保存，Cloudflare 会在两分钟内自动重新生成网站。你永远不用碰命令行。

---

# 二、整个流程一览

一共 9 步。第 1～4 步是准备文件，第 5～7 步是接入域名，**第 8 步最重要**，第 9 步是验收。

| 步骤 | 做什么 | 预计耗时 |
| --- | --- | --- |
| 1 | 注册 GitHub 账号 | 5 分钟 |
| 2 | 在 GitHub 上建一个「仓库」 | 2 分钟 |
| 3 | 把文件上传上去 | 5 分钟 |
| 4 | 填上你自己的笔名和信息 | 5 分钟 |
| 5 | 注册 Cloudflare 账号 | 5 分钟 |
| 6 | 把域名的管理权交给 Cloudflare | 10 分钟 + 等待 |
| 7 | 创建网站项目 | 10 分钟 |
| 8 | ⚠️ 关闭会拦住 AI 的开关 | 10 分钟 |
| 9 | 检查是否成功 | 5 分钟 |

**第 6 步之后需要等待**（几分钟到 24 小时不等），这段时间可以去泡杯茶。

---

# 三、开始之前：先找到你的文件夹

我已经帮你把要上传的文件整理好了，在电脑的这个位置：

```
D:\DSH_Works\0733-fun-上传用
```

**怎么打开它：**

1. 按键盘上的 `Win` 键 + `E`，会打开「文件资源管理器」
2. 在顶部的地址栏里粘贴：`D:\DSH_Works\0733-fun-上传用`
3. 按回车

你会看到这些内容（`src`、`public`、`scripts` 是文件夹，其余是文件）：

```
📁 public
📁 scripts
📁 src
📄 .gitignore
📄 astro.config.mjs
📄 DEPLOY-CLOUDFLARE.md
📄 package-lock.json
📄 package.json
📄 README.md
📄 tsconfig.json
```

> **⚠️ 重要**
> 另有一个文件夹叫 `D:\DSH_Works\0733-fun`，那是工作目录，里面有一个巨大的 `node_modules` 文件夹，**绝对不要上传它**。
> 你要上传的是名字里带「上传用」的那个。两个文件夹名字很像，注意别搞混。

---

# 第 1 步：注册 GitHub 账号

GitHub 是全球程序员存放代码的地方，免费，我们只是拿它当网盘用。

1. 打开浏览器，访问 **https://github.com/signup**
2. 依次填写：
   - **Email**：你的邮箱
   - **Password**：密码（至少 8 位，要含数字或小写字母）
   - **Username**：用户名。**只能用英文字母、数字和连字符**，比如 `zhuzhou-writer`。这个用户名以后会出现在网址里，但你自己的网站用的是 `0733.fun`，所以随便取一个好记的就行
   - 会弹出一个验证拼图的框，照着做
3. 邮箱里会收到一封验证邮件，点里面的按钮验证
4. 完成。你现在有 GitHub 账号了

> **如果页面全是英文**：浏览器右上角通常有翻译按钮（地址栏右边有个图标），点一下可以翻译成中文。**强烈建议打开翻译**，会轻松很多。
> 不过翻译有时会把按钮文字翻得奇怪，如果找不到按钮，可以对照本手册里我给出的英文原文。

---

# 第 2 步：建一个「仓库」

「仓库」（Repository，GitHub 上常简写成 repo）就是存放你这批文件的地方。

1. 登录 GitHub 后，看**页面右上角**，有一个 **`+`** 号按钮，点它
2. 在下拉菜单里选 **New repository**（新建仓库）
3. 填写：
   - **Repository name**（仓库名）：填 `0733-fun`
   - **Description**（描述）：可以不填
   - 下面有 **Public / Private** 两个选项：**选 Public**（公开）
     - 为什么必须公开：Cloudflare 的免费版需要能读取这个仓库
   - **不要勾选** `Add a README file`
   - **不要**选 `.gitignore` 模板、**不要**选 License
4. 点最下面的绿色按钮 **Create repository**

建好后你会看到一个几乎空白的页面。这就是你的「网盘」，接下来往里放文件。

---

# 第 3 步：把文件上传上去

1. 在你刚建好的空仓库页面上，找到中间那段文字，里面有一个蓝色的链接：

   > **uploading an existing file**

   点它。（如果没看到，可以直接访问这个地址，把 `<你的用户名>` 换成你的 GitHub 用户名：
   `https://github.com/<你的用户名>/0733-fun/upload/main`）

2. 页面中间会出现一个**虚线方框**，提示 `Drag files here to add them to your repository`

3. **保持浏览器窗口开着**，切换到刚才打开的那个文件夹窗口（`D:\DSH_Works\0733-fun-上传用`）

4. 在文件夹里按 **`Ctrl` + `A`** 全选所有内容（3 个文件夹 + 7 个文件，应该全部变蓝选中，**共 10 项**）

5. 用鼠标把它们**拖进浏览器的虚线方框里**，然后松手

6. 稍等几秒到几十秒（总共只有约 300 KB，很快）。列表里会出现所有文件

   > **如果拖拽没反应**：可以先把文件夹窗口缩小、和浏览器并排放，再拖。或者一个个拖也行。
   > **如果 `.gitignore` 这个文件没上传成功**：不影响网站运行，可以忽略。

7. **往下滚动到页面底部**，会看到 **Commit changes** 区域
   - 上面有个输入框，内容大概是 `Add files via upload`，**不用改**
   - 点绿色的 **Commit changes** 按钮

8. 完成后会自动跳回仓库首页。你会看到文件列表，说明上传成功了

---

# 第 4 步：填上你自己的笔名和信息

现在要修改一个配置文件，填上你的真实信息。**这一步直接在网页上改，不需要任何软件。**

## 找到文件

1. 在仓库首页的文件列表里，点 **`src`** 这个文件夹
2. 进去后点 **`site.config.ts`**
3. 看到文件内容后，点右上角那个**铅笔图标** ✏️（鼠标悬停会显示 `Edit this file`）

## 要改的地方

找到这几行，把**引号里的中文**换成你自己的内容：

```typescript
  /** 浏览器标题栏 / 首页大标题 */
  title: '0733',

  /** 一句话副标题 */
  tagline: '在湘江边写字的房间',
```

```typescript
  author: {
    /** 请填写：你的笔名 */
    name: '你的笔名',
    /** 真实姓名或本名，可留空 */
    realName: '',
    /** 身份标签，会写进 Person schema */
    jobTitle: '小说作者',
    /** 一句话简介 */
    bio: '写小说和随笔，关注南方小城的日常与衰败。',
    /** 所在地，可留空 */
    location: '湖南 株洲',
```

**具体改什么：**

| 字段 | 改成 | 说明 |
| --- | --- | --- |
| `title` | 你的网站名 | 现在是 `0733`，可以保留 |
| `tagline` | 一句副标题 | ⚠️ **我猜的，请改成你自己的** |
| `name` | **你的笔名** | ⚠️ **必改** |
| `jobTitle` | `小说作者` / `散文作者` / `编剧` 等 | 按实际写 |
| `bio` | 一句话简介 | ⚠️ **请改成你自己的** |
| `location` | 你所在的城市 | ⚠️ **我猜的，猜错请改**；不想公开就改成 `''`（两个单引号，中间什么都没有） |

> **关于「湘江」和「株洲」**：因为 0733 是株洲的电话区号，我按这个填了占位符。
> 这纯粹是我的猜测，**猜错了请务必改掉**，别让它上线。

## 保存

1. 滚动到页面底部
2. 在 **Commit changes** 的输入框里写一句话，比如 `填写我的个人信息`（写什么都可以）
3. 点绿色按钮 **Commit changes**

---

# 第 5 步：注册 Cloudflare 账号

1. 访问 **https://dash.cloudflare.com/sign-up**
2. 填邮箱和密码，点 **Sign up**
3. 去邮箱点验证链接
4. 登录后会进入 Cloudflare 的后台（一个有很多菜单的页面）

> 界面可以在右上角切换语言：点右上角头像 → `My Profile` → `Preferences` → `Language` 选「简体中文」。
> 本手册里我会把中英文都写上，找不到就对照着看。

---

# 第 6 步：把域名的管理权交给 Cloudflare

**这一步最抽象，我慢慢讲。**

## 先理解一件事

你买域名的地方（比如阿里云、腾讯云、Namecheap）有一个设置叫 **「域名服务器」**（英文 **Nameservers**，缩写 NS）。

它相当于互联网上的**电话总机**——全世界想知道「0733.fun 这个网址该去哪里找」时，都要问这个总机。

我们要做的，就是**把这个总机从你买域名的地方，转到 Cloudflare**。转过去之后，Cloudflare 才能在后台帮你管理网站的各种开关——**包括第 8 步要关掉的那些拦住 AI 的开关**。

> 为什么必须做这一步？因为 Cloudflare 的那些 AI 相关设置，只在「域名由 Cloudflare 管理」时才生效。

## 6.1 在 Cloudflare 里添加域名

1. 登录 Cloudflare 后台
2. 点 **Add a site**（添加站点）
3. 输入 `0733.fun`，点 **Continue**
4. 选择套餐：**选 Free（免费）**，点 **Continue**
5. Cloudflare 会扫描一下现有的解析记录，**直接点 Continue 跳过**
6. 接下来 Cloudflare 会给你**两个域名服务器地址**，长得像这样：

   ```
   yourname.ns.cloudflare.com
   othername.ns.cloudflare.com
   ```

   **把这两个地址复制下来**（可以先粘到记事本里）。这一步很重要。

## 6.2 到你买域名的地方改成这两个地址

**先去找到你在哪买的域名**——翻一下邮箱，搜 `0733.fun`，找当时购买成功的邮件。

登录那家网站的后台，找到 **DNS 设置** 或 **域名服务器 / Nameservers** 的地方：

| 你在哪买的 | 大致路径 |
| --- | --- |
| **阿里云 / 万网** | 控制台 → 域名 → 找到 `0733.fun` → 点「管理」→ 左侧「DNS 修改」→ 选「修改 DNS 服务器」 |
| **腾讯云** | 控制台 → 域名注册 → 我的域名 → `0733.fun` 后面点「管理」→ 「DNS 服务器」→ 修改 |
| **Namecheap** | Dashboard → Domain List → `0733.fun` 右边的 Manage → 上方 **Nameservers** 区域 → 选 **Custom DNS** → 填入两个地址 |
| **GoDaddy** | 我的产品 → 找到域名 → DNS → 找到「名称服务器」→ 更改 → 选「我自己的名称服务器」 |
| **NameSilo** | Domain Manager → `0733.fun` → 点 **Change Nameservers** |
| **Cloudflare 自己买的** | 无需操作，直接跳到 6.3 |

**操作要点：**

1. 把原来的域名服务器**全部删掉**
2. 填入 Cloudflare 给你的**那两个**地址
3. 保存

> ⚠️ **千万不要删错**：只改「域名服务器 / Nameservers」这一项。
> 不要动邮箱相关的记录，否则你的邮箱可能会收不到信。

## 6.3 回 Cloudflare 等待生效

1. 回到 Cloudflare 后台，点 **Check nameservers now**（立即检查域名服务器）
2. 状态会显示 **Pending**（等待中）——**这是正常的**
3. Cloudflare 生效后会给你发一封邮件，标题大概是 `0733.fun is now active on Cloudflare`
4. 后台里这个域名的状态会变成 **Active**（已激活）

**要等多久？** 通常几分钟到几小时，最长 24 小时。**这段时间你可以继续做第 7 步**，不用干等。

---

# 第 7 步：创建网站项目

1. 在 Cloudflare 后台，**左侧菜单**找到 **Workers & Pages**（计算 → Workers 和 Pages），点进去
2. 点 **Create**（创建）按钮
3. 在出现的选项里，选 **Pages** 标签页，然后点 **Connect to Git**（连接到 Git）
4. 会弹出 GitHub 授权窗口：
   - 点 **Authorize**（授权）
   - 如果让你选择授权范围，选 **All repositories** 或只勾选 `0733-fun` 都可以
5. 回到 Cloudflare，在列表里选中 **0733-fun** 这个仓库，点 **Begin setup**（开始设置）
6. 填写构建设置：

   | 字段 | 填什么 |
   | --- | --- |
   | **Project name**（项目名） | `0733-fun` |
   | **Production branch**（生产分支） | `main` |
   | **Framework preset**（框架预设） | 选 **Astro** |
   | **Build command**（构建命令） | `npm run build` |
   | **Build output directory**（构建输出目录） | `dist` |

   > 选了 `Astro` 之后，后两项通常会自动填好，**核对一下是否和上表一致**。

7. **展开 Environment variables (advanced)**（环境变量），添加一个：

   | Variable name | Value |
   | --- | --- |
   | `NODE_VERSION` | `22` |

8. 点 **Save and Deploy**（保存并部署）

9. Cloudflare 开始构建，大约 1～3 分钟。成功后你会看到一个类似 `0733-fun.pages.dev` 的网址——**点开它，网站已经能看了**（只不过现在还在临时地址上）

> **如果构建失败**：页面上会有红色的错误信息。最常见的原因是第 4 步改配置时改坏了引号（比如把单引号 `'` 写成了中文引号 `‘’`）。回到第 4 步检查一下，改完保存，Cloudflare 会自动重新构建。

## 7.1 绑定你自己的域名

1. 进入刚建好的项目 → 上方标签点 **Custom domains**（自定义域）
2. 点 **Set up a domain**（设置域）
3. 输入 `0733.fun`，点 **Continue** → **Activate domain**
4. 等几分钟，证书签发完成后，`https://0733.fun` 就能打开了

> 如果这一步报错说域名没激活，说明第 6 步还没生效，等一会儿再试。

---

# 第 8 步：⚠️ 关掉会拦住 AI 的开关

**这一步是整件事的核心。** 这些开关大多**默认就是拦 AI 的**，不关掉的话，网站能打开、能被 Google 搜到，但 AI 读不到你的文章——这正是你建这个站要避免的事。

**前提**：第 6 步必须已经生效（域名状态是 Active），否则你的域名下不会出现这些菜单项。

在 Cloudflare 后台，**先点进 `0733.fun` 这个域名**（不是 Workers & Pages），然后在左侧菜单里逐项核对：

| # | 位置 | 要改成 |
| --- | --- | --- |
| 1 | **Security**（安全性）→ **Settings**（设置）→ 筛选里选 **Bot traffic** → 找到 **Set your preference to block training in robots.txt** | **关闭** ⬅️ **最重要** |
| 2 | **Security** → **Bots**（机器人）→ **Bot Fight Mode** | **关闭** |
| 3 | **Security** → **Bots** → **AI Labyrinth** | **关闭** |
| 4 | 域名 **Overview**（概览）页面 → 找 **Control AI Crawlers**（控制 AI 爬虫）→ 一键拦截的开关 | **保持放行 / 关闭** |
| 5 | **Security** → **Settings** → **Under Attack Mode**（我受到攻击模式） | **关闭** |
| 6 | **Security** → **Settings** → **Security Level**（安全级别） | 不要选 **High**，选 **Medium** 或 **Essentially Off** |
| 7 | **AI Crawl Control**（AI 爬虫控制）→ **Crawlers** 标签 → 每个爬虫的 **Action** 列 | 全部选 **Allow**（允许） |

## 为什么第 1 项最重要

如果你把它**打开**，Cloudflare 会在你的 `robots.txt` 文件**开头插入**它自己写的一段规则，明确写上「禁止 GPTBot、ClaudeBot、Google-Extended、Bytespider、CCBot……抓取」。

这些正是让 AI 认识你、引用你的爬虫。**开着它，等于把门焊死。**

本站在代码里已经自带了正确的 `robots.txt`（全站放行），所以**必须让 Cloudflare 不要插手**。

## 找不到菜单怎么办

Cloudflare 的界面每隔几个月就会调整一次，菜单名字可能略有不同。如果找不到某一项：

- 在左侧菜单顶部的**搜索框**里直接搜关键词，比如搜 `AI`、`robots`、`Bot`
- 或者直接访问：`https://dash.cloudflare.com/?to=/:account/:zone/ai` —— 这是 AI 爬虫控制台的直达链接

**找不到的话，随时把你在哪个页面、看到什么告诉我，我帮你定位。**

---

# 第 9 步：检查是否成功

## 9.1 用一个在线工具查（推荐，最简单）

1. 浏览器打开 **https://isitagentready.com**
2. 在输入框里填 `0733.fun`，回车
3. 它会给你一个评分和一份清单，告诉你有哪一项没通过

这是 Cloudflare 官方文档里推荐的检测工具。

## 9.2 用浏览器直接看

在浏览器地址栏输入这几个地址，逐个打开：

| 地址 | 应该看到什么 |
| --- | --- |
| `https://0733.fun` | 你的网站首页 |
| `https://0733.fun/robots.txt` | 一段纯文字，里面有 `Allow: /`。⚠️ **绝对不能出现 `<!DOCTYPE html>` 或 `<html>` 这种字样** |
| `https://0733.fun/llms.txt` | 一个给 AI 看的站点清单 |
| `https://0733.fun/sitemap-index.xml` | 一段 XML 代码 |
| `https://0733.fun/guide/ruhe-bei-ai-yinyong/` | 那篇示例指南文章 |

**`robots.txt` 那一项要特别看。** 如果里面出现了 HTML 标签，或者出现了 `# BEGIN Cloudflare Managed content` 字样，说明第 8 步没做干净，回去关掉那个开关。

## 9.3 让我帮你查

都做完之后**告诉我一声**，我可以用真实的 AI 爬虫身份去实测你的网站，逐个确认 ChatGPT、Claude、Perplexity、豆包的爬虫能不能进来。这个检查我这边几秒钟就能跑完。

---

# 以后怎么发新文章

网站上线后，发文章**不用再碰 Cloudflare**，只要在 GitHub 上操作：

1. 打开你的仓库 `https://github.com/<你的用户名>/0733-fun`
2. 点进 `src` → `content` → `guide`（指南）或 `blog`（文章）
3. 点右上角 **Add file** → **Create new file**（新建文件）
4. 文件名填英文小写字母和连字符，**结尾必须加 `.md`**，例如 `wode-2026-nian` → 填 `wode-2026-nian.md`
5. 把下面这段**模板**粘进去，改成你自己的内容：

```markdown
---
title: "文章标题"
summary: "把最希望被 AI 引用的一句话写在这里。要求：包含你的名字、具体数字和日期，脱离上下文也能读懂。"
description: "一句话说明这篇文章讲什么。"
kind: essay
pubDate: 2026-10-01
tags:
  - 随笔
draft: false
---

## 小标题

正文从这里开始写。
```

6. 往下滚动，点绿色按钮 **Commit changes**
7. **等 1～2 分钟**，Cloudflare 会自动重新生成网站，刷新 `https://0733.fun` 就能看到新文章了

## 模板字段说明

| 字段 | 填什么 |
| --- | --- |
| `title` | 标题，**必须用英文双引号 `"` 包起来** |
| `summary` | **最重要**——可被 AI 直接引用的一句话 |
| `description` | 给搜索引擎和 AI 看的一句话简介 |
| `pubDate` | 发布日期，格式必须是 `年-月-日` |
| `tags` | 标签，一行一个，前面加两个空格和减号 |
| `draft` | `true` = 不发布（草稿），`false` = 发布 |

> ⚠️ **最容易出错的地方**：引号必须是**英文引号** `"`，不能是中文引号 `“”`。
> 打不出来就把输入法切到英文状态再打。

---

# 术语小词典

| 词 | 意思 |
| --- | --- |
| **命令行 / 终端 / PowerShell** | 那种黑底白字、要打字下命令的窗口。**本手册全程用不到。** |
| **仓库 / Repository / repo** | GitHub 上存放一批文件的地方，相当于一个文件夹 |
| **Commit** | 保存并记录一次修改。GitHub 网页上的绿色按钮 |
| **Branch / 分支** | 文件的版本线，默认叫 `main` |
| **Git** | 版本管理工具。**你不用装，GitHub 网页自带。** |
| **DNS** | 把网址翻译成服务器地址的系统 |
| **Nameserver / 域名服务器 / NS** | DNS 的「总机」，第 6 步要改的就是它 |
| **zone / 区域** | Cloudflare 里「一个由它管理的域名」。第 6 步做完就有了 |
| **CDN** | 内容分发网络，让世界各地访问你的网站都更快。Cloudflare 就是干这个的 |
| **WAF** | 网站防火墙，会拦截「看起来可疑」的访问。**AI 爬虫经常被它误伤** |
| **robots.txt** | 放在网站根目录的一个纯文本文件，用来告诉爬虫「哪些可以抓、哪些不行」 |
| **爬虫 / Bot / Spider** | 自动访问网站、读取内容的程序。搜索引擎和 AI 都靠它 |
| **UA / User-Agent** | 爬虫访问网站时自报的身份标识 |
| **结构化数据 / JSON-LD / schema** | 藏在网页里、给机器看的说明文字，告诉它「这是一篇文章」「这是作者」 |
| **sitemap / 站点地图** | 一份列出网站全部页面地址的清单，方便搜索引擎收录 |
| **RSS** | 一种订阅格式，读者和其他系统可以订阅你的更新 |
| **llms.txt** | 一份专门给 AI 看的站点清单。不是正式标准，但加上没坏处 |
| **canonical** | 告诉搜索引擎「这个页面的正式地址是这一个」 |
| **缓存** | 把内容临时存起来加快访问。改了东西没生效时，可以试试强制刷新（`Ctrl` + `F5`） |

---

# 出错了怎么办

**不要慌，也不用重头再来。** 大部分问题都能单独修。

| 现象 | 原因 | 怎么办 |
| --- | --- | --- |
| Cloudflare 构建失败、有红字 | 十有八九是第 4 步或发文章时改坏了引号 | 检查 `site.config.ts` 或那篇文章的 frontmatter，引号必须是英文 `"` `'` |
| `0733.fun` 打不开 | 第 6 步没生效 | 回 Cloudflare 看域名状态是不是 Pending / Active |
| 打开了但显示不安全 / 证书错误 | 证书还在签发 | 等 10～30 分钟 |
| 打开显示 522 错误 | 域名解析没配好 | 检查第 7.1 步是否走完 |
| `robots.txt` 里有 HTML | 第 8 步第 1 项没关 | 关掉托管 robots.txt |
| 网站能开但 AI 读不到 | 第 8 步没做干净 | 做完第 8 步，然后让我帮你实测 |
| 改了 GitHub 上的文件但网站没变 | 构建需要 1～2 分钟 | 等一会儿，然后 `Ctrl` + `F5` 强制刷新 |
| 完全卡住了 | — | **把屏幕上看到的内容告诉我**，我帮你判断 |

---

# 你现在可以做的第一件事

打开这个文件夹，确认里面的东西和手册第三节描述的一致：

```
D:\DSH_Works\0733-fun-上传用
```

确认好了，就开始第 1 步——注册 GitHub 账号。

**任何一步卡住，直接把屏幕上的内容或者报错信息发给我就行。**
